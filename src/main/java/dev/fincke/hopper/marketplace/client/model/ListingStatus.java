package dev.fincke.hopper.marketplace.client.model;

// * Enum
// Normalized listing lifecycle states shared between Java and Go layers.
public enum ListingStatus
{
    PENDING,
    ACTIVE,
    FAILED
}
